name: First CI Pipeline

on:
  push:
    branches:
      - main
      - dev

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: List Files
        run: ls -altr

      - name: Echo Message
        run: echo "Pipeline is working"
